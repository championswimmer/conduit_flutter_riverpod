import 'package:flutter/material.dart';

class GlobalFeedPage extends StatelessWidget {
  const GlobalFeedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Global Feed Page"),
    );
  }
}
